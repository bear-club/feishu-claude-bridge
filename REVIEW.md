# 实现方案审查意见

> 审查日期：2026-05-21
> 适用范围：单人使用，飞书工作群公用机器人，个人账户模式

---

## P0 — 必须在对应阶段实现前解决

### 1. 工作目录白名单应前置到第二阶段

**风险**：`/new` 允许用户手动输入任意路径，而 `allowedTools` 包含 `Bash`、`Write`、`Edit`，等于通过飞书消息可以在本机任意目录执行命令。白名单放在第五阶段，意味着第二到第四阶段的开发和测试期间完全无防护。

**建议**：在第二阶段引入 Claude 调用时，同步实现路径白名单校验。可以先用 `.env` 中的 `ALLOWED_DIRS` 配置项做最简实现，第五阶段再增强。

**阶段**：第二阶段

### 2. Session 恢复失败的降级策略

**风险**：`sessions.json` 中存储的 `sessionId` 可能因服务重启、Claude CLI 重启、session 过期等原因失效。当前方案没有 fallback——`resume` 失败后会抛异常，用户看到的是"正在执行..."永远不更新。

**建议**：`resume` 调用时 try-catch，失败则自动新建 session 并通知用户"上下文已重置"。更新 `sessions.json` 中的 sessionId。

**阶段**：第三阶段

### 3. 错误兜底：所有异步路径必须更新飞书消息

**风险**：当前方案中，Claude 调用是异步的，用户先收到"正在执行..."。如果 Claude SDK 抛异常、超时、进程崩溃，没有 catch 逻辑去更新这条消息，用户会永远卡在等待状态。

**建议**：所有调用 Claude 的异步路径都包裹 try-catch-finally，失败时更新飞书消息为错误提示。

**阶段**：第二阶段

### 4. 飞书消息去重

**风险**：飞书 WebSocket 在网络波动时可能推送重复事件。如果不去重，同一条用户消息会触发两次 Claude 调用，浪费资源且产生混乱的回复。

**建议**：用 `message_id` 做幂等判断，维护一个最近处理过的 message_id 集合（内存 Set，保留最近 1000 条即可）。

**阶段**：第二阶段

---

## P1 — 应在对应阶段一并实现

### 5. 输出截断按字节数而非字符数

**风险**：`truncateOutput` 按 28000 字符截断，但飞书卡片限制是 30KB。中文 UTF-8 每字符 3 字节，28000 中文字符 ≈ 84KB，远超限制。发送会失败。

**建议**：用 `Buffer.byteLength(text, 'utf8')` 按字节计算，卡片内容控制在 28KB 以内（留 2KB 给卡片结构 JSON）。

**阶段**：第五阶段

### 6. 消息更新频率节流

**风险**：流式输出实时更新飞书卡片时，飞书消息更新 API 有频率限制（约 5 次/秒/应用）。如果 Claude 输出速度快，频繁 PATCH 会被限流甚至封禁。

**建议**：对卡片更新做 throttle，最多每 2-3 秒更新一次。最终结果单独做一次完整更新。

**阶段**：第五阶段

### 7. API 费用监控与上限告警

**风险**：API Key 用户按 token 计费，单次复杂任务可达 $5-$20，长对话上下文累积后费用递增。通过飞书发消息无感知地触发调用，容易在不知不觉中产生高额费用。官方数据显示 API 费用可达订阅方案的 10-20 倍。

**建议**：
- 每次调用结束后记录 `total_cost_usd`，累计到 session 和全局两个维度
- 配置每日/每月费用上限（`.env` 中 `DAILY_COST_LIMIT_USD`、`MONTHLY_COST_LIMIT_USD`），触达后拒绝新请求并通知用户
- 添加 `/cost` 指令，查看当前 session 和全局累计花费
- 默认使用 Sonnet 而非 Opus（费用差 3-5 倍），需要时通过指令切换

**阶段**：第四阶段

### 8. 任务队列设置上限

**风险**：当前队列无深度限制。如果 Claude 执行卡住（如等待交互式 Bash 输入），后续所有消息会无限堆积，用户体验是"机器人完全没反应"且无任何提示。

**建议**：队列最大深度设为 3-5，超出时回复用户"当前有任务排队中，请稍后再试"。

**阶段**：第四阶段

### 9. 优雅退出处理

**风险**：当前无 `SIGTERM`/`SIGINT` 处理。强制退出时正在执行的 Claude 任务会中断，`sessions.json` 可能写入不完整导致数据损坏。

**建议**：注册进程信号处理器，退出前 abort 所有活跃任务、等待 sessions.json 写入完成。

**阶段**：第四阶段

### 10. JSON 文件读写的并发安全

**风险**：多个异步操作可能同时读写 `sessions.json`。Node.js 虽然是单线程，但异步 I/O 交错执行时，"读→修改→写"操作之间可能插入其他写操作，导致后写覆盖先写的修改。

**建议**：对 `sessions.json` 的写操作加一个简单的异步锁（Promise 串行化），或改用 SQLite（`better-sqlite3` 同步 API，天然无此问题）。

**阶段**：第三阶段

---

## P2 — 建议实现，可视情况调整

### 11. Claude CLI 可用性前置检查

**风险**：SDK 底层依赖本机 Claude Code CLI。如果 CLI 未安装、未登录、token 过期，第一次调用才会报错，排查成本高。

**建议**：服务启动时执行一次 `claude --version` 检测，失败则打印明确错误并退出。

**阶段**：第二阶段

### 12. 非文本消息反馈

**风险**：用户发送图片、文件、表情包等非文本消息时，当前代码静默忽略（`if (message_type !== "text") return`），用户不知道机器人为什么没反应。

**建议**：对非文本消息回复"暂时只支持文本消息"。

**阶段**：第二阶段

### 13. 操作审计日志

**风险**：没有记录谁在什么时间执行了什么命令、Claude 返回了什么。出问题时无法溯源。

**建议**：写入本地日志文件，记录时间戳、chat_id、用户输入摘要、执行结果摘要。可以用 `console.log` + 日志文件重定向的最简方案。

**阶段**：第四阶段

### 14. Windows 路径一致性

**风险**：README 示例用 `D:\projects\backend`，代码示例用 `/your/project/path`。实际运行在 Windows 上，路径分隔符不一致可能导致匹配、比较、存储时出现 bug。

**建议**：所有路径存储前统一用 `path.resolve()` 规范化。白名单比较时也先规范化再比对。

**阶段**：第三阶段

---

## 飞书平台限制备忘

以下是飞书自身的限制，开发时需注意但无法从应用侧解决：

| 限制 | 说明 |
|------|------|
| 卡片消息大小 | 单条卡片消息上限约 30KB |
| 普通文本消息 | 单条约 4000 字符 |
| 消息更新频率 | 约 5 次/秒/应用，频繁调用会被限流 |
| 卡片回调超时 | 按钮回调需 3 秒内返回新卡片 JSON，否则用户看到错误 |
| WebSocket 重连 | SDK 内置重连机制，但极端网络环境下可能断连丢消息 |
| 事件推送 | 网络波动时可能推送重复事件，需应用侧去重 |
| Bot 权限 | 需申请 `im:message`、`im:message:send_as_bot`、`im:resource` 等权限并通过审批 |
| 群聊 @触发 | 工作群中需要 @机器人 才触发消息事件，私聊直接触发 |

---

## 各阶段待办汇总

| 阶段 | 新增项 |
|------|--------|
| 第二阶段 | #1 路径白名单、#3 错误兜底、#4 消息去重、#11 CLI 检查、#12 非文本反馈 |
| 第三阶段 | #2 Session 降级、#10 JSON 并发安全、#14 路径规范化 |
| 第四阶段 | #7 费用监控与上限告警、#8 队列上限、#9 优雅退出、#13 审计日志 |
| 第五阶段 | #5 字节截断、#6 更新节流 |
